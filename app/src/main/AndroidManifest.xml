<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alarm.diet_ver1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Splash Activity -->
        <activity
            android:name=".Activities.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity android:name=".Activities.MainActivity" />
        <activity android:name=".Activities.Fragment1_home" />
        <activity android:name=".Activities.Fragment1.Fragment1_Banner1" />
        <activity android:name=".Activities.Fragment1.Fragment1_Banner2" />
        <activity android:name=".Activities.Fragment1.Fragment1_Banner3" />
        <activity android:name=".Activities.Fragment2.DietTestActivity" />
        <activity android:name=".Activities.Fragment2.TestResultActivity"></activity>
    </application>

</manifest>